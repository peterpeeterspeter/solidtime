<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Required for network requests -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Required for reading/writing app data -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>

    <!-- Required for activity monitoring -->
    <key>com.apple.security.automation.apple-events</key>
    <true/>

    <!-- Required for system tray -->
    <key>com.apple.security.app-sandbox</key>
    <true/>

    <!-- Hardened Runtime -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- Allow unsigned executable memory (required for some frameworks) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Disable library validation (required for loading frameworks) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
